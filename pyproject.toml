[project]
name = "pipewire-recorder"
version = "0.1.0"
description = ""
authors = [
    {name = "Steve Daulton",email = "SteveDaulton@users.noreply.github.com"}
]
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
]

[tool.poetry]
packages = [{include = "pipewire_recorder", from = "src"}]


[tool.poetry.group.dev.dependencies]
pylint = "^3.3.7"
flake8 = "^7.2.0"
pytest = "^8.3.5"
mypy = "^1.15.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
